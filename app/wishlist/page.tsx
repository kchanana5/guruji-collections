import Link from "next/link";
import { redirect } from "next/navigation";
import { createClient } from "@/lib/supabase/server";
export default async function WishlistPage(){const supabase=await createClient();const {data:{user}}=await supabase.auth.getUser();if(!user)redirect("/account/login?next=/wishlist");const {data:items}=await supabase.from("wishlists").select("id,products(id,name,slug,brand,base_price)").eq("user_id",user.id).order("created_at",{ascending:false});return <main className="min-h-screen bg-[var(--background)] px-5 py-10"><div className="mx-auto max-w-6xl"><Link href="/account" className="text-sm font-semibold">← Account</Link><p className="mt-8 text-xs font-bold uppercase tracking-[.3em] text-[var(--accent)]">GJC ACCOUNT</p><h1 className="mt-2 text-4xl font-black">Wishlist</h1>{!items?.length?<div className="mt-8 rounded-3xl border border-dashed bg-white p-12 text-center"><p className="font-bold">Your wishlist is empty.</p><Link href="/shop" className="mt-5 inline-flex rounded-xl bg-[#171717] px-5 py-3 text-sm font-bold text-white">Explore GJC</Link></div>:<div className="mt-8 grid gap-4 sm:grid-cols-2 lg:grid-cols-4">{items.map((row:any)=><Link key={row.id} href={`/shop/${row.products.slug}`} className="rounded-2xl border bg-white p-5"><p className="font-bold">{row.products.name}</p><p className="mt-1 text-xs text-black/45">{row.products.brand||"Guruji Collections"}</p><p className="mt-3 font-black">₹{Number(row.products.base_price).toLocaleString("en-IN")}</p></Link>)}</div>}</div></main>}
