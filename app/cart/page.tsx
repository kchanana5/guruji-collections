"use client";
import Link from "next/link"; import { useEffect, useMemo, useState } from "react";
type Item={variantId:string;quantity:number;product:{id:string;name:string;slug:string;price:number;image:string|null};variant:{size:string|null;color:string|null;price:number;stock:number}};
export default function CartPage(){const[items,setItems]=useState<Item[]>([]);useEffect(()=>{const read=()=>setItems(JSON.parse(localStorage.getItem("gjc-cart")||"[]"));read();window.addEventListener("gjc-cart-updated",read);return()=>window.removeEventListener("gjc-cart-updated",read)},[]);const total=useMemo(()=>items.reduce((s,i)=>s+i.variant.price*i.quantity,0),[items]);function update(index:number,quantity:number){const next=[...items];if(quantity<=0)next.splice(index,1);else next[index]={...next[index],quantity:Math.min(quantity,next[index].variant.stock)};setItems(next);localStorage.setItem("gjc-cart",JSON.stringify(next));window.dispatchEvent(new Event("gjc-cart-updated"));}function clearItem(index:number){const next=items.filter((_,i)=>i!==index);setItems(next);localStorage.setItem("gjc-cart",JSON.stringify(next));window.dispatchEvent(new Event("gjc-cart-updated"));}
return <main className="min-h-screen bg-[var(--background)]"><header className="border-b border-[var(--border)] bg-white"><div className="mx-auto flex max-w-7xl items-center justify-between px-4 py-4 sm:px-5 sm:py-5 lg:px-8"><Link href="/" className="flex items-center gap-3"><span className="flex h-11 w-11 items-center justify-center rounded-full bg-[var(--brand)] text-sm font-bold tracking-widest text-white">GJC</span><span className="hidden text-sm font-bold tracking-[0.25em] sm:block">GURUJI</span></Link><Link href="/shop" style={{color:"#171717",backgroundColor:"#fff"}} className="rounded-xl border border-black/10 px-4 py-2.5 text-sm font-semibold">Continue shopping</Link></div></header><section className="mx-auto max-w-6xl px-4 py-8 sm:px-5 sm:py-10 lg:px-8"><p className="text-xs font-bold uppercase tracking-[0.3em] text-[var(--accent)]">GJC BAG</p><h1 className="mt-2 text-4xl font-semibold">Your cart</h1>{!items.length?<div className="mt-10 rounded-3xl border border-dashed border-black/10 bg-white p-10 text-center sm:p-12"><p className="font-semibold">Your cart is empty.</p><Link href="/shop" style={{color:"#fff",backgroundColor:"#171717"}} className="mt-5 inline-flex rounded-xl px-5 py-3 text-sm font-bold">Shop GJC</Link></div>:<div className="mt-8 grid gap-6 lg:grid-cols-[1fr_320px]"><div className="space-y-4">{items.map((item,index)=><div key={item.variantId} className="flex flex-col gap-4 rounded-2xl border border-black/10 bg-white p-4 sm:flex-row"><div className="h-28 w-24 shrink-0 overflow-hidden rounded-xl bg-stone-100">{item.product.image&&<img src={item.product.image} alt={item.product.name} className="h-full w-full object-cover"/>}</div><div className="min-w-0 flex-1"><div className="flex items-start justify-between gap-3"><Link href={`/shop/${item.product.slug}`} className="font-semibold">{item.product.name}</Link><button type="button" onClick={()=>clearItem(index)} style={{color:"#171717",backgroundColor:"#fff"}} className="rounded-lg border border-black/10 px-3 py-2 text-xs font-semibold">Remove</button></div><p className="mt-1 text-xs text-black/50">{item.variant.size||"One size"}{item.variant.color?` · ${item.variant.color}`:""}</p><p className="mt-3 text-sm font-bold">₹{item.variant.price.toLocaleString("en-IN")}</p><div className="mt-3 flex items-center gap-2"><button type="button" aria-label="Decrease quantity" onClick={()=>update(index,item.quantity-1)} style={{color:"#171717",backgroundColor:"#fff"}} className="h-9 w-9 rounded-lg border border-black/15 text-lg">−</button><span className="w-7 text-center text-sm font-semibold">{item.quantity}</span><button type="button" aria-label="Increase quantity" onClick={()=>update(index,item.quantity+1)} style={{color:"#171717",backgroundColor:"#fff"}} className="h-9 w-9 rounded-lg border border-black/15 text-lg">+</button></div></div></div>)}</div><aside className="h-fit rounded-2xl border border-black/10 bg-white p-5 sm:p-6"><p className="text-xs font-bold uppercase tracking-[.25em] text-[var(--accent)]">Order summary</p><div className="mt-5 flex justify-between text-sm"><span>Subtotal</span><span className="font-bold">₹{total.toLocaleString("en-IN")}</span></div><div className="mt-4 flex justify-between border-t pt-4 text-sm"><span>Shipping</span><span className="text-black/50">Calculated at checkout</span></div><div className="mt-4 flex justify-between border-t pt-4 font-bold"><span>Total</span><span>₹{total.toLocaleString("en-IN")}</span></div><p className="mt-3 text-xs leading-5 text-black/50">Taxes included. Shipping calculated at checkout.</p><Link href="/checkout" style={{color:"#fff",backgroundColor:"#171717"}} className="mt-6 flex min-h-14 w-full items-center justify-center rounded-xl px-5 py-4 text-sm font-bold">Proceed to checkout →</Link><div className="mt-4 flex justify-center gap-3 text-xs text-black/50"><span>✓ Secure checkout</span><span>•</span><span>↻ Easy returns</span></div></aside></div>}</section></main>}
