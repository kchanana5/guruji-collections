"use client";
import Link from "next/link"; import { useEffect, useMemo, useState } from "react";
type Item={variantId:string;quantity:number;product:{id:string;name:string;slug:string;price:number;image:string|null};variant:{size:string|null;color:string|null;price:number;stock:number}};
export default function CartPage(){const[items,setItems]=useState<Item[]>([]);useEffect(()=>{const read=()=>setItems(JSON.parse(localStorage.getItem("gjc-cart")||"[]"));read();window.addEventListener("gjc-cart-updated",read);return()=>window.removeEventListener("gjc-cart-updated",read)},[]);const total=useMemo(()=>items.reduce((s,i)=>s+i.variant.price*i.quantity,0),[items]);function update(index:number,quantity:number){const next=[...items];if(quantity<=0)next.splice(index,1);else next[index]={...next[index],quantity:Math.min(quantity,next[index].variant.stock)};setItems(next);localStorage.setItem("gjc-cart",JSON.stringify(next));}
return <main className="min-h-screen bg-[var(--background)]"><header className="border-b border-[var(--border)] bg-white"><div className="mx-auto flex max-w-7xl items-center justify-between px-5 py-5 lg:px-8"><Link href="/" className="flex items-center gap-3"><span className="flex h-11 w-11 items-center justify-center rounded-full bg-[var(--brand)] text-sm font-bold tracking-widest text-white">GJC</span><span className="text-sm font-bold tracking-[0.25em]">GURUJI</span></Link><Link href="/shop" className="text-sm font-semibold">Continue shopping</Link></div></header><section className="mx-auto max-w-5xl px-5 py-10 lg:px-8"><p className="text-xs font-bold uppercase tracking-[0.3em] text-[var(--accent)]">GJC BAG</p><h1 className="mt-2 text-4xl font-semibold">Your cart</h1>{!items.length?<div className="mt-10 rounded-3xl border border-dashed border-black/10 bg-white p-12 text-center"><p className="font-semibold">Your cart is empty.</p><Link href="/shop" className="mt-5 inline-flex rounded-xl bg-[#171717] px-5 py-3 text-sm font-bold text-white">Shop GJC</Link></div>:<div className="mt-10 grid gap-8 lg:grid-cols-[1fr_320px]"><div className="space-y-4">{items.map((item,index)=><div key={item.variantId} className="flex gap-4 rounded-2xl border border-black/10 bg-white p-4"><div className="h-28 w-24 overflow-hidden rounded-xl bg-stone-100">{item.product.image&&<img src={item.product.image} alt={item.product.name} className="h-full w-full object-cover"/>}</div><div className="min-w-0 flex-1"><Link href={`/shop/${item.product.slug}`} className="font-semibold">{item.product.name}</Link><p className="mt-1 text-xs text-black/50">{item.variant.size||"One size"}{item.variant.color?` · ${item.variant.color}`:""}</p><p className="mt-3 text-sm font-bold">₹{item.variant.price.toLocaleString("en-IN")}</p><div className="mt-3 flex items-center gap-2"><button onClick={()=>update(index,item.quantity-1)} className="h-8 w-8 rounded-lg border">−</button><span className="w-6 text-center text-sm">{item.quantity}</span><button onClick={()=>update(index,item.quantity+1)} className="h-8 w-8 rounded-lg border">+</button></div></div></div>)}</div><aside className="h-fit rounded-2xl border border-black/10 bg-white p-6"><div className="flex justify-between text-sm"><span>Subtotal</span><span className="font-bold">₹{total.toLocaleString("en-IN")}</span></div><p className="mt-3 text-xs leading-5 text-black/50">Shipping and taxes are calculated at checkout.</p><Link href="/checkout" className="mt-6 block w-full rounded-xl bg-[#171717] px-5 py-3.5 text-center text-sm font-bold text-white">Checkout — ₹{total.toLocaleString("en-IN")}</Link></aside></div>}</section></main>}
